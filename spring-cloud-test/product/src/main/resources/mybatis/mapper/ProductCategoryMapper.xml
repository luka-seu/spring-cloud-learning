<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.plasticlove.product.dao.ProductCategoryDao">


	<select id="findByCategoryTypeIn" resultType="cn.plasticlove.product.entity.ProductCategory">
		select * from product_category
		where category_type in
		/*传入参数为List的处理方法*/
		<foreach collection="categoryTypeList" index="index" item="type" open="(" separator="," close=")">
			#{type}
		</foreach>
	</select>
</mapper>
 

